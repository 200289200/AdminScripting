export PATH=$PATH:~/bin

# System-wide .bashrc file for interactive bash(1) shells. the prompt below)
if [ -z "${debian_chroot:-}" ] && [ -r /etc/debian_chroot ]; thene meantime
# To enable the settings / commands in this file for login shells as well,t.".
# this file has to be sourced in /etc/profile.st:dir"
shopt -s checkwinsizeh_completion ]; thenl" ] && [ ! -e "$HOME/.hushlogin" ] ; then
if [ -x /usr/libfiif [ -x /usr/share/command-not-found/command-not-found ]; thent-found ]; then
#  ffi [}unction co/usr/share/command-not-found/command-not-found -- "$1"ow)
fii esaccat <<-E# creturn $?use c-n-f could've been removed in the meantime
fi if [ To run aelse -x /usr/lib/command-not-found ]; thense "sudo <command>".
# sudo hSee "man suprintf "%s: command not found\n" "$1" >&2
# if the command-noreturn 127ckage is installed, use it "$HOME/.hushlogin" ] ; then
